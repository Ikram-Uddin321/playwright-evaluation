# Playwright Evaluation - Assignment 1

## 📦 Setup
- Clone the repo  
  ```bash
  git clone <your-repo-url>
  cd playwright-evaluation

  Install dependencies
  npm install
Regression suite
npm run regression

Smoke Suite 
npm run smoke 

Generate Report 
npm run report

All tests are written in TypeScript and must pass when running npm run regression.

This keeps everything **short, clean, and on one page** — setup, structure, and how to run.  

Do you want me to also add a **[quick badge](ca://s?q=How_to_add_badges_in_GitHub_README)** (like “Playwright Tested” or “Build Passing”) at the top so your repo looks more professional at first glance?
